/** Lock só vale com a tela de inspeção carregada (heartbeat). */
export const INSPECTION_LOCK_TTL_MS = 45_000;
/** Renovação enquanto a inspeção da OP está aberta. */
export const INSPECTION_LOCK_HEARTBEAT_MS = 15_000;
